{
  "type": "applet",
  "name": "Maps",
  "description": "Search for locations",
  "entrypoint": "index.html",
  "actions": [
    {
      "id": "search_nearby",
      "description": "Search for places nearby the origin location",
      "params": {
        "origin": {
          "type": "string",
          "description": "The location to search near"
        },
        "query": {
          "type": "string",
          "description": "The search query"
        }
      }
    }
  ]
}
