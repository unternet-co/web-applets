{
  "scripts": {
    "postinstall": "cd playground && npm install && cd ../sdk && npm install",
    "chat": "npm run applets:build && cd chat && npm run dev",
    "playground": "nodemon --watch applets --ignore 'applets/*/dist/*' --exec \"npm run applets:build && npm run playground:dev\" -e ts,html,css,js",
    "playground:dev": "cd playground && npm run dev",
    "applets:build": "node scripts/build-all.js && node scripts/write-manifest.js",
    "applets:compile": "tsc -p tsconfig.applets.json",
    "applets:copy-files": "rsync -a --exclude='*.ts' applets/ dist/applets/"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.7",
    "vite": "^5.4.8"
  }
}
