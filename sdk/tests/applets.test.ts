import { describe, expect, it } from '#tests/common/tooling.js';
import { AppletFactory } from '#src/applets/applet-factory.js';

describe("Applets", () => {
  describe("sendAction", () => {
    it("gets the result", async () => {
      // TODO: Cannot work with `Window` instances here.
      //       The applet code should probably use something different.
    })
  })
})
